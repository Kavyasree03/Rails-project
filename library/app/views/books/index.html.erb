<p id="notice"><%= notice %></p>

<i><h1><font color=#950348>Library_Books</font></h1></i>

<font color=#5B2C6F ><table>
  <thead>
    <tr>
      <th>Book name</th>
      <th>Author name</th>
      <th>Genere</th>
      <th>Description</th>
      <th>Book code</th>
      <th>Books taken</th>
      <th>Books remaining</th>
      <th>Total books</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.book_name %></td>
        <td><%= book.author_name %></td>
        <td><%= book.genere %></td>
        <td><%= book.Description %></td>
        <td><%= book.book_code %></td>
        <td><%= book.books_taken %></td>
        <td><%= book.books_remaining %></td>
        <td><%= book.total_books %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= button_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table></font>

<br>

<%= link_to 'New Book', new_book_path %>
